spring:
  datasource:
    url: jdbc:h2:mem:puncher_test;MODE=PostgreSQL;DB_CLOSE_DELAY=-1
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: create-drop
  mail:
    host: localhost
    port: 3025

app:
  jwt:
    secret: test-secret-key-at-least-256-bits-long-for-jwt-hmac-sha256-signing
  cors:
    allowed-origins: http://localhost:3000
